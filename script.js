/* First, add the code here */

cat_image = document.querySelector('img');

cat_image.addEventListener('click', ()=> {
 
  // add css that makes the cat dissapear
})







setTimeout(() => {  document.querySelector('.temp').style = "display: none"

/*  Then, move the code here instead. */
  
}, 3000)